Archived assets that are not referenced by live routes or generated content anymore.

These files are kept out of `public/` so they do not inflate the deployed asset footprint,
but they remain in-repo in case we need to restore them later.
